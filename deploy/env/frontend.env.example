# Frontend build-time env (placed at frontend/.env.production)
# Same-origin reverse proxy in production — relative paths only.

NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_APP_URL=https://panel.example.com
NEXT_PUBLIC_WS_URL=https://panel.example.com
NEXT_PUBLIC_WS_PATH=/socket.io
